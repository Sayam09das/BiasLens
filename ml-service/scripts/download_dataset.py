"""Dataset download entry point.

This project currently keeps source datasets in `data/raw/`.
Use this script as the future home for authenticated or remote dataset fetches.
"""

from __future__ import annotations


def main() -> None:
    print("Datasets are already present locally in ml-service/data/raw.")


if __name__ == "__main__":
    main()
