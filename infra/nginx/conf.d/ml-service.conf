upstream biaslens_ml_service {
  server ml-service:8000;
}

server {
  listen 80;
  location /ml/ {
    proxy_pass http://biaslens_ml_service;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
}
