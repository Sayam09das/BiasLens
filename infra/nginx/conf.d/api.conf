upstream biaslens_backend {
  server backend:4000;
}

server {
  listen 80;
  location /api/ {
    proxy_pass http://biaslens_backend;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
}
